use std::pin::Pin;

use crate::core::transposer::transposer_update::TransposerUpdateInner;


#[test]
fn test_input() {
    // let update = TransposerUpdate::new_input(frame, time, inputs, state);
    // let update = Pin::new(&mut update);

    // update.poll(poll_time, input_state, cx);
}